{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile all in bin dir",
            "type": "shell",
            "command": "javac src/* -d bin",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}